FIND(A,v)
for i <- 1 to length[A]
    do if A[i] = v
            then i;
       i <- i+1
return NIL

在第一轮迭代开始之前，A[]为空集，没有元素的值等于v。
每i轮结束之后，A[1..i]中没有元素的值等于v
循环结束时，i = length[A]+1,也就是说A[1..length[A]]中没有元素的值等于v。

